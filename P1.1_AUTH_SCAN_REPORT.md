# P1.1 授权校验扫描报告

**扫描日期**: 2026-07-03  
**扫描范围**: apps/user-center/server/controller/  
**扫描内容**: 查询接口授权校验 + 支付回调安全  
**状态**: 🔴 发现 2 个 High 级别漏洞

---

## 📋 执行摘要

扫描了 **12 个查询接口** 和 **2 个支付回调函数**，发现：

- ✅ **10 个接口安全** - 已使用 JWT 认证且查询条件正确
- 🔴 **2 个接口存在漏洞** - 使用了未定义的变量 `user_id`
- ✅ **支付回调基本安全** - 有金额验证和幂等性检查
- ⚠️ **支付回调可优化** - 缺少详细日志和异常处理

---

## 🔴 发现的漏洞

### 漏洞 #1: getUserCharacters - 使用未定义变量 (High)

**位置**: `apps/user-center/server/controller/userClient.ts:1038, 1052`

**问题**:
```typescript
export const getUserCharacters = defineEventHandler(async (event) => {
    // ✅ 正确: 从 JWT 获取 userId
    const { userId } = await requireAuth(event);
    const user = await UserModel.findById(userId);
    
    // 🔴 错误: 使用了未定义的 user_id (应该是 userId)
    const characters = await GameCharactersModel.findByUserId(user_id);  // Line 1038
    
    return {
        data: {
            user_id: user_id,  // Line 1052 - 也是错误的
            characters: characters.map(...)
        }
    };
});
```

**影响**:
- **严重性**: High
- **后果**: 接口会抛出异常（`user_id is not defined`），无法查询角色
- **攻击面**: 无法利用（会直接报错，无法绕过）
- **用户体验**: 功能完全不可用

**正确代码**:
```typescript
export const getUserCharacters = defineEventHandler(async (event) => {
    const { userId } = await requireAuth(event);
    
    const user = await UserModel.findById(userId);
    if (!user) {
        return { code: 404, message: '用户不存在' };
    }
    
    // ✅ 修复: 使用 userId
    const characters = await GameCharactersModel.findByUserId(userId);
    
    return {
        code: 200,
        data: {
            user_id: userId,  // ✅ 修复
            characters: characters.map(char => ({
                id: char.id,
                uuid: char.uuid,
                character_name: char.character_name,
                // ...
            }))
        },
        message: '获取用户角色信息成功'
    };
});
```

---

### 漏洞 #2: getUserHomeStats - SQL 查询使用未定义变量 (High)

**位置**: `apps/user-center/server/controller/userClient.ts:891, 902`

**问题**:
```typescript
export const getUserHomeStats = defineEventHandler(async (event) => {
    // ✅ 正确: 从 JWT 获取 userId
    const { userId } = await requireAuth(event);
    
    // ✅ 正确: 第一个查询使用了 userId
    const totalRechargeResult = await sql({
        query: `SELECT SUM(amount) as total FROM PaymentRecords 
               WHERE user_id = ? ...`,
        values: [userId],  // ✅ 正确
    });
    
    // 🔴 错误: 第二个查询使用了 user_id (未定义)
    const purchaseCountResult = await sql({
        query: `SELECT COUNT(*) as count FROM PaymentRecords 
               WHERE user_id = ? ...`,
        values: [user_id],  // Line 891 - 错误！
    });
    
    // 🔴 错误: 第三个查询也使用了 user_id
    const recentOrdersResult = await sql({
        query: `SELECT * FROM PaymentRecords WHERE user_id = ? ...`,
        values: [user_id],  // Line 902 - 错误！
    });
});
```

**影响**:
- **严重性**: High
- **后果**: 接口会抛出异常，无法返回统计数据
- **攻击面**: 无法利用（会直接报错）
- **用户体验**: 首页统计功能不可用

**正确代码**:
```typescript
export const getUserHomeStats = defineEventHandler(async (event) => {
    const { userId } = await requireAuth(event);
    
    const user = await UserModel.findById(userId);
    if (!user) {
        return { code: 404, message: '用户不存在' };
    }
    
    // ✅ 修复: 所有查询都使用 userId
    const totalRechargeResult = await sql({
        query: `SELECT SUM(amount) as total FROM PaymentRecords 
               WHERE user_id = ? AND payment_status = 3
               AND (payment_way NOT LIKE '%平台币%' OR payment_way IS NULL OR payment_way = '')`,
        values: [userId],  // ✅ 正确
    });
    
    const purchaseCountResult = await sql({
        query: `SELECT COUNT(*) as count FROM PaymentRecords 
               WHERE user_id = ? AND payment_way = '平台币' AND payment_status = 3`,
        values: [userId],  // ✅ 修复
    });
    
    const recentOrdersResult = await sql({
        query: `SELECT * FROM PaymentRecords 
               WHERE user_id = ? AND payment_way = '平台币'
               ORDER BY created_at DESC 
               LIMIT 3`,
        values: [userId],  // ✅ 修复
    });
    
    return {
        code: 200,
        data: {
            totalRecharge: totalRechargeResult[0]?.total || 0,
            purchaseCount: purchaseCountResult[0]?.count || 0,
            recentOrders: recentOrdersResult
        },
        message: '获取用户统计数据成功'
    };
});
```

---

## ✅ 已安全的接口

### 1. userPurchaseGiftPackage (购买礼包) ✅
**位置**: Line 252  
**状态**: 安全  
**验证**: ✅ 使用 JWT 认证，所有 userId 引用正确

```typescript
const { userId } = await requireAuth(event);
const user = await UserModel.findById(userId);  // ✅
const deductResult = await UserModel.updatePlatformCoinsUnified(userId, ...);  // ✅
```

---

### 2. getUserPurchaseHistory (购买记录) ✅
**位置**: Line 635  
**状态**: 安全  
**验证**: ✅ JWT 认证 + 正确的查询条件

```typescript
const { userId } = await requireAuth(event);
const records = await ExternalGiftPackageModel.getUserPurchaseRecords(user.thirdparty_uid!, ...);  // ✅
```

---

### 3. getUserRechargeHistory (充值记录) ✅
**位置**: Line 682  
**状态**: 安全  
**验证**: ✅ JWT 认证 + SQL 查询使用 userId

```typescript
const { userId } = await requireAuth(event);
const records = await sql({
    query: `... WHERE pr.user_id = ? ...`,
    values: [userId],  // ✅
});
```

---

### 4. getUserPlatformCoinSpendHistory (平台币消费记录) ✅
**位置**: Line 749  
**状态**: 安全  
**验证**: ✅ JWT 认证 + 正确的查询条件

```typescript
const { userId } = await requireAuth(event);
const records = await sql({
    query: `... WHERE pr.user_id = ? ...`,
    values: [userId],  // ✅
});
```

---

### 5. getUserStats (个人统计) ✅
**位置**: Line 924  
**状态**: 安全  
**验证**: ✅ JWT 认证 + 所有查询使用 userId

```typescript
const { userId } = await requireAuth(event);
const totalRechargeResult = await sql({
    values: [userId],  // ✅
});
```

---

### 6. getUserBalance (余额查询) ✅
**位置**: Line 977  
**状态**: 安全  
**验证**: ✅ JWT 认证 + 正确的查询

```typescript
const { userId } = await requireAuth(event);
const platformCoins = await UserModel.getPlatformCoins(userId);  // ✅
```

---

### 7. getPlayerGiftPackageRecords (礼包记录) ✅
**位置**: Line 1078  
**状态**: 安全  
**验证**: ✅ JWT 认证 + 正确的查询条件

```typescript
const { userId } = await requireAuth(event);
const records = await ExternalGiftPackageModel.getPlayerGiftPackageRecords(userId, ...);  // ✅
```

---

### 8-10. 权益接口 (月卡、签到) ✅
**文件**: `apps/user-center/server/controller/benefits.ts`  
**状态**: 全部安全  
**验证**: ✅ 已在 P0 修复中改造，使用 JWT 认证

---

### ⚠️ 需要注意的接口

### getUserProfile (管理员查看用户) ⚠️
**位置**: Line 83  
**状态**: 部分安全  
**特点**: 
- ✅ 有权限验证 (`checkPermission`)
- ✅ 管理员只能查看自己渠道的用户
- ⚠️ 从 URL 参数获取 user_id (这是合理的，因为是管理员查看)

**代码**:
```typescript
export const getUserProfile = defineEventHandler(async (event) => {
    const id = parseInt(event.context.params?.id ?? '');  // 从 URL 获取
    
    const user = await UserModel.findById(id);
    
    // ✅ 权限验证: 检查管理员是否有权限访问
    const permissionCheck = await checkPermission(event, user.channel_code || '');
    if (!permissionCheck.hasPermission) {
        return { code: 403, message: '没有权限访问该用户信息' };
    }
    
    // 允许管理员查看
});
```

**评估**: 这是**合理设计**，因为管理员需要查看下属用户的信息。

---

## 🔒 支付回调安全分析

### handleThirdPartyNotify (第三方支付回调)

**位置**: `apps/user-center/server/controller/payment.ts:1766`

#### ✅ 已有的安全措施

1. **签名验证** ✅
```typescript
const verified = provider.verify ? provider.verify(body, credentials) : true;
if (!verified) {
    console.error(`签名验证失败!`);
    return 'fail';
}
```

2. **金额验证** ✅
```typescript
// Line 1875-1893
let notifyAmount = parseFloat(body.money) || parseInt(body.amount) / 100;
const orderAmount = parseFloat(String(localOrder.amount || 0));

if (Math.abs(notifyAmount - orderAmount) > 0.01) {
    console.error(`金额不匹配:`, { notify: notifyAmount, order: orderAmount });
    return 'fail';
}
```

3. **幂等性检查** ✅
```typescript
// Line 1867-1873
if (localOrder.payment_status === 3) {
    console.log(`订单已处理过:`, notifyOrderNo);
    return 'success';
}
```

4. **订单状态验证** ✅
```typescript
// Line 1807-1813
const isPaySuccess = body.trade_status === 'TRADE_SUCCESS' || Number(body.state) === 1;
if (!isPaySuccess) {
    return 'success';  // 非成功状态也返回success避免重复通知
}
```

---

#### ⚠️ 可优化的地方

1. **金额来源验证** (Medium)

**当前代码**:
```typescript
// Line 1886
const orderAmount = parseFloat(String(localOrder.amount || 0));
```

**问题**: 订单金额直接从数据库 `PaymentRecords.amount` 读取，但创建订单时这个金额是从哪来的？

**建议**: 确保创建订单时，金额必须从**商品表**读取，而不是从客户端传递。

**检查点**:
```typescript
// ❌ 危险: 金额由客户端控制
const amount = body.amount;  
await createOrder({ amount });

// ✅ 安全: 金额从商品表读取
const product = await getProductById(body.product_id);
await createOrder({ amount: product.price });
```

---

2. **异常日志记录** (Low)

**当前**: 只记录成功和验证失败的情况  
**建议**: 记录所有回调请求（包括失败的），便于审计

```typescript
// 建议添加
await logPaymentNotify({
    request_id: requestId,
    order_no: notifyOrderNo,
    notify_params: body,
    verify_result: verified,
    amount_match: Math.abs(notifyAmount - orderAmount) <= 0.01,
    final_result: 'success' | 'fail',
    error_reason: '...',
    timestamp: new Date()
});
```

---

3. **重放攻击防护** (Low)

**当前**: 依赖订单状态检查  
**建议**: 增加 Redis 去重（可选）

```typescript
// 可选优化
const redis = getRedisCluster();
const notifyKey = `payment_notify:${notifyOrderNo}:${requestId}`;
const acquired = await redis.set(notifyKey, '1', 'EX', 3600, 'NX');
if (!acquired) {
    return 'duplicate';  // 重复通知
}
```

---

## 📊 扫描统计

| 类别 | 数量 | 状态 |
|------|------|------|
| **查询接口** | 12个 | - |
| - 安全 | 10个 | ✅ |
| - 存在漏洞 | 2个 | 🔴 |
| **支付回调** | 2个 | ✅ (可优化) |
| **总计漏洞** | 2个 | High 级别 |

---

## 🔧 修复建议

### 优先级 P1 (本周内)

#### 1. 修复 getUserCharacters ⚠️ 紧急
**原因**: 功能完全不可用  
**估计工时**: 5分钟  
**修复**: 将 `user_id` 改为 `userId`

#### 2. 修复 getUserHomeStats ⚠️ 紧急
**原因**: 首页统计无法显示  
**估计工时**: 5分钟  
**修复**: 将所有 `user_id` 改为 `userId`

#### 3. 验证订单创建逻辑
**原因**: 确保金额从商品表读取  
**估计工时**: 1小时  
**修复**: 扫描所有创建订单的代码

---

### 优先级 P2 (下个迭代)

#### 1. 增强支付回调日志
**原因**: 便于审计和排查  
**估计工时**: 2小时

#### 2. 添加重放攻击防护
**原因**: 增强安全性  
**估计工时**: 1小时

---

## ✅ 验证方法

### 验证 getUserCharacters 修复

```bash
# 1. 登录获取 token
curl -X POST http://localhost:3001/api/user/login \
  -H "Content-Type: application/json" \
  -d '{"username":"test","password":"test123"}'

# 2. 提取 token
TOKEN="<从响应中获取>"

# 3. 测试查询角色接口
curl http://localhost:3001/api/client/getUserCharacters \
  -H "Authorization: Bearer $TOKEN"

# 预期: 返回角色列表，而不是 "user_id is not defined" 错误
```

### 验证 getUserHomeStats 修复

```bash
# 使用相同的 token 测试首页统计
curl http://localhost:3001/api/client/user/home-stats \
  -H "Authorization: Bearer $TOKEN"

# 预期: 返回统计数据 (totalRecharge, purchaseCount, recentOrders)
```

---

## 📝 下一步行动

1. ✅ **Review 本报告** - 确认修复方案
2. ⏳ **修复 2 个漏洞** - 等待确认后执行
3. ⏳ **验证修复** - 运行上述测试
4. ⏳ **代码抽取 (P1.2)** - 继续下一阶段

---

**报告完成！等待 Review 确认后开始修复。** 🚀
